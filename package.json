{
  "name": "table-column-resize",
  "version": "1.0.3",
  "private": true,
  "scripts": {
    "build": "esbuild src/main.ts --bundle --external:obsidian --external:@codemirror/state --external:@codemirror/view --external:@codemirror/language --outfile=main.js --format=cjs --platform=node --target=es2018"
  },
  "devDependencies": {
    "@codemirror/state": "^6.0.0",
    "@codemirror/view": "^6.0.0",
    "esbuild": "^0.20.0",
    "obsidian": "latest",
    "typescript": "^5.0.0"
  }
}
